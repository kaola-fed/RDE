FROM {{{from}}}

WORKDIR {{{workDir}}}

RUN npm i -g rde

COPY .cache/package.json {{{dockerWorkDirRoot}}}/package.json

{{!cache node-modules}}
RUN npm i

{{!cache .runtime dir}}
{{#isApp}}
COPY . .

RUN rde docker:run
{{/isApp}}
