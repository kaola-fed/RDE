FROM {{{from}}}

WORKDIR {{{workDir}}}

COPY .cache/package.json {{{dockerWorkDirRoot}}}/package.json

{{!cache node-modules}}
RUN npm i & npm i -g git+https://git@github.com/visionmedia/express.git#develop

{{!cache .runtime dir}}
{{#isApp}}
COPY . .

RUN rde docker:run
{{/isApp}}
