FROM {{{from}}}

WORKDIR {{{workDir}}}

COPY .cache/package.json {{{dockerWorkDirRoot}}}/package.json

{{!cache node-modules}}
RUN npm i && npm i -g rde

{{^isApp}}
COPY . .
{{/isApp}}

{{!cache .runtime dir}}
RUN rde docker:run
