FROM {{{from}}}

WORKDIR {{{workDir}}}

COPY .cache/package.json {{{dockerWorkDirRoot}}}/package.json

{{!cache node-modules}}
RUN npm i && npm i -g rde

{{#isContainer}}
COPY . .
{{/isContainer}}

{{!cache .runtime dir}}
RUN rde docker:run
