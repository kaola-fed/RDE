version: '3'
services:
  rde:
    build:
      context: '../'
      dockerfile: ./.docker/Dockerfile
    volumes:
    - ../app:./app
    - ../template:./template
    ports:
    {{#ports}}
    - "{{.}}"
    {{/ports}}
    command: rde docker:run {{{cmd}}} {{#watch}}--watch{{/watch}}
